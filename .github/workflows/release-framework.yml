name: Release framework NPM package

run-name: Release ${{ github.ref_name }}

on:
  push:
    tags:
      - framework@v[0-9]+.[0-9]+.[0-9]+
      - framework@v0.0.0-dev.[0-9]+

jobs:
  release:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./framework
    steps:
      - name: Get tag
        id: tag
        run: |
          tag=${{ github.ref_name }}
          version=${tag:11}
          dev=${{ startsWith(github.ref_name, 'framework@v0.0.0-dev') }}
          echo "tag=$tag" >> $GITHUB_OUTPUT
          echo "version=$version" >> $GITHUB_OUTPUT
          echo "dev=$dev" >> $GITHUB_OUTPUT

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: yarn

      - name: Compile
        run: yarn compile

      - name: Lint
        run: yarn lint

      - name: Build
        run: yarn build

      - name: Package
        run: yarn package ${{ steps.tag.outputs.version }}

      - name: Publish to NPM
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          cd build
          npm publish \
            --tag=${{ steps.tag.outputs.dev == 'true' && 'dev' || 'latest' }}
