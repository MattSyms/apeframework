name: Release ESLint NPM Package

run-name: Release ${{ github.ref_name }}

on:
  push:
    tags:
      - eslint@v[0-9]+.[0-9]+.[0-9]+
      - eslint@v0.0.0-dev.[0-9]+

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Get tag
        id: tag
        run: |
          tag=${{ github.ref_name }}
          version=${tag:8}
          dev=${{ startsWith(github.ref_name, 'eslint@v0.0.0-dev') }}
          echo "tag=$tag" >> $GITHUB_OUTPUT
          echo "version=$version" >> $GITHUB_OUTPUT
          echo "dev=$dev" >> $GITHUB_OUTPUT

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        working-directory: ./eslint
        run: yarn

      - name: Compile
        working-directory: ./eslint
        run: yarn compile

      - name: Lint
        working-directory: ./eslint
        run: yarn lint

      - name: Build
        working-directory: ./eslint
        run: yarn build

      - name: Package
        working-directory: ./eslint
        run: yarn package ${{ steps.tag.outputs.version }}

      - name: Publish to NPM
        working-directory: ./eslint
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          cd build
          npm publish \
            --tag=${{ steps.tag.outputs.dev == 'true' && 'dev' || 'latest' }}
